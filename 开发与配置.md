# 开发与配置

面向开发者 / 高级用户的文档：源码构建、全部配置项、模型说明、目录结构。

普通用户不需要看这篇——直接下载 [Releases](https://github.com/Tianxiaorui9001/VRCVoice/releases) 里的分发版即可。

## 源码运行

```bash
git clone https://github.com/Tianxiaorui9001/VRCVoice.git
cd VRCVoice

python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt

# 使用本地识别时下载并校验官方模型；只使用云端后端可跳过
models\download_model.bat

start.bat
# 或
.venv\Scripts\python.exe main.py
```

## 打包

```powershell
.venv\Scripts\python.exe -m pip install -r requirements-build.txt
powershell -ExecutionPolicy Bypass -File build_dist.ps1
```

- 当前版本由仓库根目录的 `VERSION` 定义
- 输出：`dist\VRCVoice\VRCVoice.exe`（onedir）与 `dist\VRCVoice_v<版本>.zip`
- 打包配置：`VRCVoice.spec`（PyInstaller）
- 构建脚本只读取仓库中的源码、资源和模型，不会读取或修改 `%APPDATA%\VRCVoice`
- 需要复现本项目验证过的完整依赖集合时，使用 `.venv\Scripts\python.exe -m pip install --require-hashes -r requirements.lock`

## 自动发布

合并到 `master` 时若 `VERSION` 发生变化，GitHub Actions 会在 Windows runner 上按照
`requirements.lock` 创建环境、下载并校验语音模型、运行打包脚本，然后创建对应的
`v<版本>` 标签和 GitHub Release。已有同名 Release 时工作流会停止，不会覆盖产物。

发布新版本只需在 PR 中修改 `VERSION`，例如从 `0.9.1` 改为 `0.9.2`。也可从 GitHub
Actions 页面手动运行同一工作流；手动运行仍会读取 `VERSION` 并执行重复版本检查。

## 配置

打包版配置位于 `%APPDATA%\VRCVoice\config.json`；源码运行时配置位于仓库根目录的 `config.json`。两者均在首次运行时自动生成，GUI 设置面板修改即保存。全部字段：

| 配置 | 说明 |
|---|---|
| recognition.backend | local(本地模型) / cloud(云端API) |
| recognition.cloud_endpoint | 云端 ASR 接口(默认硅基流动 OpenAI 兼容) |
| recognition.cloud_api_key | 云端 API Key |
| recognition.cloud_model | 云端模型, 如 FunAudioLLM/SenseVoiceSmall |
| general.language | 识别语言 zh / en / zh-en (本地模式) |
| recognition.model_dir | 本地模型目录（默认 models/） |
| recognition.mic_device | 麦克风设备名（VR 时可选 PicoStreamingMicrophone） |
| recognition.max_duration_sec | 单次录音上限（防误触） |
| recognition.silence_stop_sec | 静音自动停止（0=关） |
| trigger.mode | hold / toggle |
| trigger.pc_hotkey | pynput 键名，如 right_ctrl、f8、ctrl+shift+v |
| trigger.vr_action | SteamVR action 名 |
| output.mode | osc / keyboard / both |
| output.osc_host / osc_port | VRChat OSC（默认 127.0.0.1:9000） |
| output.osc_typing_indicator | 录音时 /chatbox/typing |
| output.keyboard_enter_send | 键盘模式自动回车 |
| polish.* | 可选润色 API（OpenAI 兼容接口） |
| ai_models.list[].capability | 模型用途：`speech`=语音识别，`text`=润色/翻译 |
| ai_models.list[].endpoint | 该模型自己的请求地址；语音通常以 `/v1/audio/transcriptions` 结尾，文字通常以 `/v1/chat/completions` 结尾 |

> 注意：仓库里的 `config.json` 被 `.gitignore` 排除（含本机设置），不要提交自己的配置。

### 云端模型用途

「AI 设置」中的每个模型条目都必须选择用途：

- `speech`：仅出现在“设置 → 云端模型”中。连通测试会上传一段 0.5 秒 WAV，并以 multipart 表单实际调用语音转写接口。
- `text`：仅出现在“AI 润色”和“AI 翻译”的模型下拉中。连通测试会实际调用聊天补全接口。

旧版模型条目首次加载时会按 API 地址和常见模型名自动补上用途。API Key 只保存在本机配置中；不要把 `config.json`、日志或密钥提交到 Git。

## 模型

默认模型：sherpa-onnx-streaming-zipformer-bilingual-zh-en（中英双语流式，int8，约 280MB）。

下载脚本：

```
models\download_model.bat
```

脚本从 sherpa-onnx 官方 GitHub Release 下载模型，只提取运行所需文件，并逐个校验固定 SHA-256；模型本体不会提交到 Git。

模型文件需为：

```
encoder-epoch-99-avg-1.int8.onnx
decoder-epoch-99-avg-1.onnx
joiner-epoch-99-avg-1.int8.onnx
tokens.txt
bpe.vocab
```

### 本地 vs 云端

| | 本地 (sherpa-onnx) | 云端 (OpenAI 兼容) |
|---|---|---|
| 网络 | 不需要 | 需要 |
| 占用 | ~500MB 模型 | 0 |
| 准确率 | 良好 | 更高 |
| 适合 | 离线 / 隐私敏感 | 低配 / 空间紧张机器 |

## 目录结构

```
VRCVoice/
├─ main.py              入口
├─ VERSION              当前发行版本号
├─ config.json          配置(自动生成, 不入库)
├─ requirements.txt     固定版本的运行依赖
├─ requirements-build.txt 构建依赖入口
├─ requirements.lock    含传递依赖和哈希的可复现环境
├─ models/              下载脚本入库，ASR 模型本体不入库
├─ resources/           SteamVR action manifest + 绑定文件
│  ├─ action_manifest.json
│  └─ bindings_{default,hmd,oculus,pico,vive,wmr}.json
├─ Language/            多语言 UI(zh-CN/zh-TW/en-US/ja)
├─ app/
│  ├─ settings.py       配置读写
│  ├─ recorder.py       录音
│  ├─ asr_engine.py     sherpa-onnx 流式识别
│  ├─ cloud_asr.py      云端 ASR 客户端
│  ├─ output.py         OSC / 键盘输出
│  ├─ hotkey.py         PC 全局热键
│  ├─ vr_input.py       OpenVR 输入(Phase 2)
│  ├─ vr_trigger.py     SteamVR action 绑定触发
│  ├─ vr_overlay.py     VR 悬浮窗
│  ├─ desktop_overlay.py 桌面悬浮窗
│  ├─ vrc_status.py     VRChat OSC 状态检测
│  ├─ controller.py     识别流程控制器
│  ├─ i18n.py           国际化
│  └─ gui/              Fluent 设置面板
├─ build_dist.ps1       PyInstaller 打包脚本
└─ VRCVoice.spec        PyInstaller 配置
```

## 技术栈

- Python 3.12 + PySide6 + PyInstaller
- [sherpa-onnx](https://github.com/k2-fsa/sherpa-onnx) 本地流式识别
- [qfluentwidgets](https://github.com/zhiyiYo/PyQt-Fluent-Widgets) Fluent 风格 UI
- [pythonnet-openvr](https://github.com/0x24A/pythonnet-openvr) SteamVR 输入
- python-osc：OSC 输出 / VRChat 状态
- pynput：PC 全局热键
