# 未发布社区修复 — 云端语音/文字模型分离

## 🔧 修复

- 模型条目新增 `speech` / `text` 用途，识别、润色和翻译下拉按用途过滤
- 硅基流动与 OpenAI 分别保存语音转写和文字聊天默认地址
- 连通测试按用途发送真实聊天请求或 multipart WAV，不再用聊天 JSON 测试语音接口
- 云端识别错误只显示在软件界面和日志中，不再发送到 VRChat 聊天框
- 旧模型配置会自动推断用途并迁移

---

# VRCVoice v0.9.13 — Quest / Virtual Desktop 手柄修复

## 🔧 修复

- **Quest / Oculus Touch 左摇杆按下无响应**：修正 SteamVR 默认绑定的输入源，由错误的 Knuckles `thumbstick/click` 改为 Oculus Touch `joystick`，并使用 joystick 模式读取 click
- **升级后仍使用旧手柄绑定**：启动时按内容哈希同步内置 SteamVR action manifest 和绑定文件，确保新版修复能覆盖 `%APPDATA%\VRCVoice\resources` 中的旧副本

## 🩺 诊断

- VR 日志新增 SteamVR runtime、action handles、控制器角色、`controller_type`、input profile、型号和渲染模型；若特定串流驱动仍不响应，可通过 `%APPDATA%\VRCVoice\vrcvoice.log` 继续定位

---
# VRCVoice v0.9.12 — 自定义润色风格修复

## 🔧 修复

- **自定义润色风格被输出守卫误伤**：预设风格（轻度/仅整理/正式）保持严格守卫防注入；自定义风格放宽到 max(8x, x+200)——用户指定要风格化输出时（如角色扮演、卖萌风格等。用户指定风格化输出时），长度扩写是正常现象，不再误判“跑题”回退原文。服务器级越狱输出仍会被拦截

## 📦 其他

- 预设文件移出仓库跟踪（presets/ 不再包含在源码仓库中，仅作为本地/内置示例）

---
# VRCVoice v0.9.11 — 翻译 / 稳定性修复

## 🔧 修复

- **修复启动崩溃**：语言文件中 7 个自动更新相关词条的值误存为嵌套结构，界面初始化读取后类型错误导致启动即崩溃，已还原并全量校验（四语言 398 条均正常）
- **i18n 全面修复**：补齐 29 个缺失的翻译 key（热词对话框等界面此前一直显示原始文案）；修正触发模式说明的错字（按住说话、松开发送）
- **打包版语言同步**：构建流程正确打包语言文件目录，已安装版本的语言改动可正常生效，不再出现"改了不生效"

## 🎁 调整

- **"识别语言"标题与提示拆分**：提示文字（目前只支持中英…）独立显示为设置项副文本，不再与标题挤在同一行

---

# VRCVoice v0.9.10 — 自动更新里程碑版

本版正式加入**软件内自动更新**全流程，并对安装链路做了彻底加固。

## ✨ 新增

- **检查更新**：关于页一键检查最新版本，直连优先、网络异常自动回退代理
- **自动下载**：实时进度条、可随时取消、SHA-256 完整性校验
- **一键重启安装**：下载完成后自动替换程序文件并拉起新版
- **更新横幅**：启动时后台检查，发现新版在首页顶部显示橙色横幅，一键直达更新
- **CI 自动构建发布**：只改版本号即可自动构建并发布 Release

## 🔧 修复

- **更新安装"重启后软件消失"彻底修复**：
  - 修复 QApplication 未导入导致的崩溃（点"重启并安装"时主程序异常退出）
  - 修复安装脚本被 taskkill /T 连坐自杀的问题（安装不再中断）
- 下载速度过慢时自动切换代理通道重新下载
- 更新链路整体加固：路径写死进脚本、逐步留痕日志、无控制台环境下等待机制替换

## 🎯 核心能力

- **离线语音识别**（sherpa-onnx 本地模型）+ **LLM 润色补全**，实测识别率 98%+，润色 100% 正确
- **VR 悬浮窗**实时显示识别文本（SteamVR Overlay）
- **OSC 输出**：识别/润色结果直通 VRChat 等支持 OSC 的应用
- **多触发方式**：全局热键 / SteamVR 手柄按键 / PICO 摇杆 / 触摸板
- **热词机制**：自定义热词权重，游戏术语识别精准（VRC+/好友+ 等特殊词支持）
- 多语言界面（简中 / 繁中 / 英文 / 日文）
