# VRCVoice 使用说明

VRCVoice 是一个给 VRChat 里不方便说话的人用的「按住说话」工具：

> 按住手柄按键说话 → 自动识别成文字 → 发到 VRChat 聊天框

## 一、系统要求

- Windows 10 / Windows 11（64 位）
- 已安装 Steam 和 SteamVR
- 使用 VRChat（需要开启 OSC 功能）
- 支持的 VR 手柄：PICO（PICO Connect）、Index（Knuckles）、Quest/Rift（Oculus Touch）、WMR 头显

## 二、开始使用

1. 把整个文件夹解压到任意位置（不要放压缩包里直接运行）
2. 双击 `VRCVoice.exe` 启动，托盘出现话筒图标，主窗口会弹出
3. 启动 SteamVR 并进入 VRChat
4. 在 VRChat 里打开 OSC：菜单 → 设置（齿轮）→ OSC → 启用（VRChat 需要先启用 OSC 才能收到文字）
5. 按住左手柄摇杆说话，松开后文字会自动发送到聊天框
   - 也支持键盘：按住右 Ctrl 说话（可在设置里改热键）

## 三、VR 悬浮窗

识别时头显里会显示一个状态窗（蓝色 = 识别中，绿色 = 已发送）。

- **看不到？** 在设置里调「悬浮窗缩放 / 水平位置 / 垂直位置」，或关闭「发送后自动隐藏」
- **不想要悬浮窗？** 设置里关掉「VR 悬浮窗」即可，不影响语音功能

## 四、常见问题

**Q: 启动被杀毒软件拦截 / 误报？**

A: 这是未签名的小工具常见情况（PyInstaller 打包的程序经常被误报）。请点击「允许运行」，或在杀毒软件里把 `VRCVoice.exe` 加入信任列表。代码开源，可自行检查。

**Q: 按手柄没反应？**

A:
1. 确认 SteamVR 已启动且手柄已连接（SteamVR 状态窗口里能看到手柄）
2. 确认进的是 VRChat 而不是桌面
3. 确认 VRChat 的 OSC 已启用
4. 绑定默认已配好（PICO/Index/Quest/WMR），一般不需要手动改绑定
5. 如绑定界面显示「python」之类的应用，那是系统组件，不用管

**Q: 识别不准？**

A: 设置 → 识别后端选「本地」（免费、无网络）或「云端」（更准、需要网络）。说话时离麦克风近一点，环境安静一点。

**Q: 如何分别配置云端语音识别和 AI 润色/翻译？**

A: 打开「AI 设置」并分别添加模型：

1. 添加语音模型时，把“模型用途”选为「语音识别（语音转文字）」。
2. 添加文字模型时，把“模型用途”选为「文字对话（润色/翻译）」。
3. 点击「测试连通」。语音模型会发送测试 WAV，文字模型会发送测试对话。
4. 在“设置 → 识别后端”中只会看到语音模型；在“AI 润色/AI 翻译”中只会看到文字模型。

如果测试出现 HTTP 400/500，请先确认用途、接口地址与模型名相互匹配。网络错误只会显示在软件界面和日志中，不会作为聊天内容发送进 VRChat。

**Q: 专有名词（人名/游戏名等）识别不准？**

A: 用识别热词表（仅本地后端生效，云端后端不支持）。打开 `%APPDATA%\VRCVoice\hotwords.txt`（记事本即可；首次启动会自动生成），每行加一个词，可带权重：

```
VRChat 3.0
龙门石窟 2.0
```

权重越大识别时越偏向该词（建议 1.0~5.0）。带 `#` 的行是注释。改完保存并重启 VRCVoice 生效。热词不生效时，可试试提高权重（比如从 3.0 提到 5.0）。

**Q: 配置存在哪里？**

A: `%APPDATA%\VRCVoice\config.json`。删掉它会恢复默认设置；重装程序不影响配置。

**Q: 怎么开机自启？**

A: 设置 → 常规 → 开机自启。或自己把 `VRCVoice.exe` 快捷方式放进启动文件夹。

## 五、卸载

直接删除整个文件夹即可（配置在 `%APPDATA%\VRCVoice`，如要彻底删除请一并删除）。

---

版本：0.9.13（本社区修复基于此版本）
